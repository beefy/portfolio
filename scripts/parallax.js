var hesitate = 0;
var height_wait;
var pagenum = 0;

function page_parallax() {
    /*
    //if at page start
    if (window.pageYOffset % ($('#cover').height() == 0) && hesitate < 50) {
        if (hesitate == 0) height_wait = window.pageYOffset;
        window.pageYOffset = height_wait;
        hesitate++;
    } else if (hesitate >= 50) {
        hesitate = 0;
        pagenum++;
    }
    */
}